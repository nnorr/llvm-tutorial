# Build output
build/
*.o
*.a
*.so
a.out
toy

# build.sh names the binary after the source stem, so ch3.cpp -> ch3
ch[0-9]
toy[0-9]

# LLVM/Clang intermediates emitted by the tutorial
*.ll
*.bc
*.s
output.o

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json

# Editor / OS
.vscode/
.idea/
.cache/
*.swp
.DS_Store

# Upstream tutorial sources and the per-chapter working copies. Kept locally
# for diffing against this implementation, deliberately not published --
# reference/ is unmodified LLVM code and chN.cpp is barely changed from it.
reference/
ch[0-9].cpp
build.sh
